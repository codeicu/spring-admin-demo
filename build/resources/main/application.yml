spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:80801

#logging:
#  # 设置logback.xml位置
#  config: classpath:logback.xml

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS